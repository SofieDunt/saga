package controller;

import model.StoryWriterModel;
import view.LibraryApplicationView;

/**
 * Represents a controller for an application that allows users to write, or create, stories by controlling a
 * {@link StoryWriterModel} and a {@link LibraryApplicationView}. Supports importing, exporting, and
 * playing through, restarting, quitting, and removing stories.
 */
public class WriterController {

}
